# URLs dos serviços — em localhost use os defaults; na VPS troque pelo domínio/IP.
# NEXT_PUBLIC_MEDIA_URL é o único ponto a mudar para plugar uma CDN no futuro.
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_MEDIA_URL=http://localhost:4001
